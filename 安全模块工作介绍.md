下面按**已经复评过的历史 P30 配置**来讲。先提醒一个重要细节：**历史 P30 的日志配置显示 CPair additive 是开启的，系数 0.03；但当前 GitHub main 里我看到 `train_airl_baseline.py` 有一处把 `safety_cpair_additive_weight` 初始化成了 `0.0`，而且没有搜到后续更新语句。**如果你现在直接用当前仓库重跑 P30，需要让 Codex 先确认/修复这一点，否则当前代码跑出来可能真的退化成“只有安全标量融合，没有 CPair 损失纠正”。历史 P30 的 run_config 明确记录了 `PREDICTIVE_SAFETY_ENABLE_CPAIR_ADDITIVE=True`、`PREDICTIVE_SAFETY_CPAIR_ADDITIVE_COEFF=0.03`、`PREDICTIVE_SAFETY_RESIDUAL_SCALE=0.5`，而当前仓库代码片段里 `safety_cpair_additive_weight` 被置为 0 后传入 `MildSafetyAIRL`。 ([GitHub][1])

# 1. 一句话概括 P30 的安全模块

**P30 的安全模块不是 shield，也不是直接给策略动作加硬约束。它是在 AIRL 判别器/奖励网络里加入一个 state-action 安全风险标量，让判别器学 reward 时知道“这个动作未来可能危险”，再通过 very light 的 CPair ranking 约束，避免同一个状态下危险动作拿到比安全动作更高的 reward。**

更具体地说，P30 是这几个东西叠起来：

```text
old SafetyQ scalar
+ 0.5 * predictive safety residual
+ scalar-only safety fusion
+ CPair additive ranking, coeff = 0.03
+ U220 / D230 / Decay250 后期调度
```

同时，P30 关掉了这些东西：

```text
legacy safety aux / legacy safety regulator
generator risk penalty
full safety feature fusion
CandidateV2 / task-safe / raw-rank
v10 shield
```

所以你的观察是对的：**原始安全模块的“标量融合”保留了，原始的 legacy 损失纠正基本去掉了。**但要补一句：历史 P30 并不是完全没有安全损失项；它保留的是 **CPair additive ranking** 这条新的、同状态候选动作排序约束，而不是老的 batch-level / legacy aux 约束。P30 的配置里 `PREDICTIVE_SAFETY_BASE_REG_COEFF=0.0`、`PREDICTIVE_SAFETY_LATE_REG_COEFF=0.0`、`SAFETY_REGULATOR_COEFF=0.0`，但 `PREDICTIVE_SAFETY_CPAIR_ADDITIVE_COEFF=0.03`。

# 2. 安全信号从哪里来

P30 里有两个安全网络/风险来源。

第一个是**原始 SafetyQ**。它是 old safety module 的核心，会根据当前状态 `s` 和动作 `a` 输出一个风险 logit，经过 sigmoid 变成：

```text
base_risk = sigmoid(old_safety_logit)
```

第二个是**predictive safety residual**。它基于 1.0s 预测风险 oracle 训练出来，专门看短时未来的 gap / TTC / THW / overlap 风险。P30 不让它完全替代 old safety，而是用 residual scale 0.5 作为补充信号：

```text
pred_risk = sigmoid(predictive_safety_logit)
scaled_pred_risk = 0.5 * pred_risk
```

最后，P30 的有效安全风险是：

```text
q_safe_risk = max(base_risk, 0.5 * pred_risk)
```

也就是说，predictive 分支只是在 old safety 不够敏感时往上补风险；它不会直接把 old safety 覆盖掉。代码里 `_get_safety_outputs` 正是先算 old safety 的 `q_risk`，再在 predictive residual 存在且 scale > 0 时，用 `torch.maximum(q_risk, pred_risk * predictive_residual_scale)` 合并风险。([GitHub][2])

这个设计很关键。它解释了为什么 P30 比“纯 predictive 替换”稳定：**old safety 负责保底，predictive 只负责补充未来风险。**

# 3. “只保留标量融合”具体是什么意思

P30 的 `SAFETY_FUSE_FEATURE=False`、`SAFETY_EMBED_DIM=1`。这意味着安全分支不会把 SafetyQ 的隐藏特征拼进 reward net，只传一个安全标量。历史总结也明确记录，当前有效路径是 `scalar-only`，而 `SAFETY_FUSE_FEATURE=True` 的 feature fusion 曾经明显变差。

代码层面也能看到：如果 `fuse_safety_feature=False`，`safety_input_dim` 就是 1，`safety_proj` 是 `Identity()`；如果打开 feature fusion，才会把 `q_feat` 和 `q_logit` 一起输入安全投影层。P30 走的是前者，也就是只把一个风险 logit/标量送进奖励网络。([GitHub][2])

所以 P30 的 reward net 大概可以理解成两条支路：

```text
行为支路:
  state + attention feature + action
  -> behavior feature

安全支路:
  state + action
  -> old SafetyQ risk
  -> predictive residual 修正
  -> q_safe_logit / q_safe_risk
  -> 1维 safety scalar

融合:
  concat(behavior feature, safety scalar)
  -> reward / discriminator logit component
```

`SafeQAttentionRewardNet` 的代码注释也写得很直接：它是 `(s,a)` 经过 behavior branch，同时 `(s,a)` 经过 trained S-Net 得到 `q_safe(s,a)` 和 safety feature，再 concat 后输出 scalar reward/logit；而在 P30 里，这个 safety feature 实际就是 scalar-only。([GitHub][2])

# 4. 它怎么影响 AIRL 判别器

AIRL 的判别器本质上在学：专家轨迹的 `(s,a)` 应该更像 expert，生成策略的 `(s,a)` 应该更不像 expert。P30 让判别器在打分时多看一个变量：

```text
这个 state-action pair 的安全风险是多少？
```

于是同样一个动作，如果行为上看起来像专家，但安全风险高，reward net/判别器就有机会把它打低。

举个抽象公式，不一定完全等同代码实现，但直觉是：

```text
reward = f_behavior(s, a, attention)
       + f_safety(q_safe_risk)
```

因为 P30 是 scalar-only，所以 `f_safety` 的输入非常窄，就是一个风险标量。它不像 feature fusion 那样把 safety network 的高维内部特征都放进来，所以不会大幅扰乱判别器结构。

# 5. 你说的“去除了损失纠正”到底是哪一部分

这里要分三层说。

第一层，**老的 legacy aux / legacy regulator 确实被关掉了。**
P30 里：

```text
PREDICTIVE_SAFETY_BASE_REG_COEFF = 0.0
PREDICTIVE_SAFETY_LATE_REG_COEFF = 0.0
SAFETY_REGULATOR_COEFF = 0.0
```

这意味着以前那种“对 generated risky samples 额外惩罚，让判别器不要把危险样本判成 expert-like”的老式辅助损失不再起主导作用。`disc_aux_safety_loss` 这个函数在代码里仍然存在，它的逻辑是只惩罚 generated risky samples 被判别器打成 expert-like 的情况，但 P30 的对应权重是 0，所以这条 legacy 损失基本不工作。 ([GitHub][2])

第二层，**P30 历史配置里保留了 CPair additive。**
CPair 不是老的 batch-level safety aux，而是 same-state candidate-pair ranking。它对每个 generated state 构造多个候选动作，计算每个候选动作的风险，然后比较“更安全动作”和“更危险动作”的 reward。如果危险动作 reward 比安全动作 reward 高，就加 ranking loss。代码里 `_predictive_candidate_pair_loss` 只取 generated samples，并为每个 state 构造候选动作，再调用 `get_safety_debug` 计算候选动作风险。([GitHub][3])

第三层，**generator risk penalty 是关掉的。**
P30 没有直接给 PPO 的训练 reward 扣风险分：

```text
PREDICTIVE_SAFETY_GEN_PENALTY = 0.0
```

所以 P30 不是：

```text
策略做危险动作 -> 立刻 reward -= risk_penalty
```

而是：

```text
判别器/奖励网络学 reward 时看到风险标量；
如果 CPair 生效，则让危险候选动作不要比安全候选动作 reward 高；
PPO 再根据这个 learned reward 更新策略。
```

P42/P43/P44 后来测试过 generator penalty，P42/P43 因阈值太高基本没触发，P44 触发后反而打坏 P30 的 292~298 甜点，所以最终才回到 P30。P42/P43/P44 的配置分别记录了不同 penalty 设置。  

# 6. CPair 是怎么工作的

按历史 P30 设计，CPair 的核心是：

```text
同一个 state 下，不同 action 风险不同；
reward net 不应该把更危险的 action 打得比更安全的 action 高。
```

流程可以写成：

```text
1. 只拿 generated samples
2. 对每个 state 构造候选动作：
   a_policy
   a_hold_lane
   a_delay_merge
   a_mild_decel
   a_mild_accel
   a_mild_merge

3. 对每个候选动作计算 q_safe_risk
4. 选 safe action：
   safe = 风险最低的候选动作

5. 选 risky action：
   risky = 当前 policy action
   如果 policy action 风险不够高，则可能退到候选里最高风险动作

6. 计算 reward_safe 和 reward_risky
7. 如果 reward_risky 比 reward_safe 高，就惩罚
```

对应的 loss 直觉上是：

```text
loss_cpair = ReLU(margin + reward_risky - reward_safe) * risk_gap
```

其中：

```text
risk_gap = risk_risky - risk_safe
```

代码里确实会统计 `cpair_raw_risk_gap_mean`、`cpair_clipped_risk_gap_mean`、`cpair_safe_type_*_rate`、`cpair_reward_gap_mean` 等诊断项，并最终在有效样本上用 `pair_loss * risk_gap` 作为加权损失。([GitHub][3])

这就是 P30 和“只把安全标量塞给判别器”的区别。如果 CPair 生效，它不仅让判别器看到风险，还会告诉判别器：

```text
在同一个状态下，别偏爱更危险的动作。
```

但再次强调：**你当前 GitHub main 里有一处疑似把 CPair additive weight 置零的问题。**如果没有修复，那么当前代码重跑的“P30”就只剩 risk scalar input，不再有 CPair additive loss。([GitHub][1])

# 7. 举例 1：正常安全合流

假设当前状态是：

```text
ego 想向目标车道合流
目标车道前车距离足够
后车也没有快速逼近
```

策略给出动作：

```text
a_policy = 轻微左/右合流 + 轻微加速
```

安全模块可能输出：

```text
base_risk = 0.08
pred_risk = 0.20
effective_risk = max(0.08, 0.5 * 0.20)
               = max(0.08, 0.10)
               = 0.10
```

这时传给判别器的是一个较低风险标量。判别器看到：

```text
行为像专家
安全风险也低
```

于是这个动作可以拿到较高 reward。CPair 里如果候选动作包括 `hold_lane`、`mild_decel`、`mild_merge`，它们的风险可能都差不多低；此时 `risk_gap` 小，CPair loss 基本不会强行干预。

这类样本的作用是：**让模型知道“安全的合流动作是可以被奖励的”，而不是一味鼓励保守。**

# 8. 举例 2：短视 old safety 看不出，但 predictive 看到未来风险

假设当前状态是：

```text
ego 正在合流
目标车道前车距离看起来还行
但未来 0.7~1.0s 内，ego 会追近 lead 车，TTC 下降
```

如果 old SafetyQ 只看当前或很短的一步风险，它可能给出：

```text
base_risk = 0.20
```

但 predictive residual 做 1.0s rollout 后发现未来 lead gap 会很危险，给出：

```text
pred_risk = 0.90
```

P30 不直接用 0.90，而是 residual scale 0.5：

```text
effective_risk = max(0.20, 0.5 * 0.90)
               = max(0.20, 0.45)
               = 0.45
```

这时判别器不会把它当成“立刻必撞”的超高风险，但会知道这个动作有中等偏高风险。这个设计比 `effective_risk=0.90` 稳，因为它不会把所有有未来风险的合流动作都打死。

CPair 里同一个 state 可能有几个候选动作：

```text
a_policy      = 继续合流
a_mild_decel  = 稍微减速等待
a_delay_merge = 延迟合流
a_hold_lane   = 保持车道
```

风险可能是：

```text
risk(policy)      = 0.45
risk(mild_decel)  = 0.18
risk(delay_merge) = 0.22
risk(hold_lane)   = 0.15
```

如果 reward net 当前错误地给：

```text
reward(policy) = 1.2
reward(mild_decel) = 0.8
```

那 CPair 会产生惩罚，因为更危险的 `policy` 动作 reward 比更安全候选动作高。经过训练后，reward net 会更倾向于：

```text
reward(policy) ↓
reward(mild_decel / delay_merge) ↑ 或至少不低于 risky action
```

这不是部署时替换动作；只是让 AIRL 学到一个更安全的 reward 排序。

# 9. 举例 3：已经非常危险的状态

假设 ego 已经快和目标车道前车重叠，old SafetyQ 和 predictive 都认为危险：

```text
base_risk = 0.85
pred_risk = 1.00
effective_risk = max(0.85, 0.5 * 1.00)
               = 0.85
```

注意这里 effective risk 还是 0.85，而不是 0.5。因为 old safety 已经很高，predictive residual 不会降低它。

这类样本传给判别器后，安全 scalar 非常高。如果 generated policy 还把它做得像 expert，legacy aux 原本会惩罚“危险 generated 样本被判成 expert-like”。但 P30 把 legacy aux 权重设为 0，所以它主要依靠 reward net 的安全输入和 CPair 排序来间接纠正。

这也解释了为什么 P30 仍然不是最终安全解：fixed protocol 下 P30@298 还有 `26/217` full collision，hard15 还有 `11/15` 碰撞。P30 确实比旧 Decay250 更好，但没有通过固定协议 gate。

# 10. 举例 4：为什么 P30 没有变得特别保守

假设某个专家动作本来是合理合流，但 predictive oracle 有一点保守，给了：

```text
base_risk = 0.05
pred_risk = 0.40
```

P30 的融合是：

```text
effective_risk = max(0.05, 0.5 * 0.40)
               = 0.20
```

这个 0.20 是一个 warning 级别，而不是 hard unsafe。它会让判别器有所顾虑，但不会直接判死。这也是为什么 residual scale 0.5 比更大 scale 稳。

你之前的离线诊断也支持这个判断：predictive risk oracle 对 expert action 并没有大面积误报，expert action 的风险 p90 约 0.1704，正常 non-collision 样本 `risk>0.7` 约 2.49%；而 hard15 碰撞前 1 秒 `risk>0.7` 约 79.33%，说明这个 oracle 大体能区分正常动作和临撞动作。

# 11. P30 的安全模块和旧 Decay250 的区别

旧 Decay250 的安全模块主要是：

```text
old SafetyQ scalar
+ legacy safety aux/reg
+ U220 / D230 / Decay250
```

P30 则变成：

```text
old SafetyQ scalar
+ predictive residual 0.5
+ CPair additive 0.03
+ legacy aux/reg = 0
+ U220 / D230 / Decay250
```

所以它的关键变化不是“把安全模块变复杂”，而是：

```text
把安全信息从静态/短视 old scalar
升级成 old scalar + predictive future-risk residual；
再把安全纠正从 batch-level legacy aux
换成 same-state candidate-pair ranking。
```

从 fixed protocol 看，旧 Decay250@265 full217 是 `45/217` collision，P30@298 是 `26/217`；P30@292 的 hard15 碰撞数是 `10/15`，比 P30@298 的 `11/15` 稍好。说明 P30 的安全模块确实有改善，但还没解决 hard-case safety。

# 12. 为什么后来 CandidateV2、task-safe、penalty 没留下

你后面做了很多增强尝试，比如 CandidateV2、task-safe、focused CPair、generator risk penalty。它们的离线诊断看起来有道理，但训练端大多破坏了 P30 的 292~298 甜点。

比如 D32 诊断显示，在碰撞前 0.5s/0.7s，CandidateV2 lead-aware 确实能拉开更大的 raw risk gap；但 P31/P32 训练证明，`candidate_v2_leadaware + task_safe + raw-rank` 从 epoch 1 接入会破坏 P30 后期窗口。  

generator penalty 也是类似。P42/P43 因 threshold=0.7 没触发，P44 用 target-lead penalty 触发后，反而把 P30 的强窗口打坏。所以最终保留 P30 的原因是：**它是目前“足够安全改善 + 不破坏合流能力”的最稳折中。**  

# 13. 最准确的论文式描述

你可以这样描述 P30 的安全模块：

```text
We keep the original action-aware safety scalar branch and use it as a scalar-only safety feature in the AIRL discriminator. 
Instead of relying on the original legacy safety auxiliary loss, we augment the scalar with a predictive residual risk estimated from a 1.0s short-horizon safety critic. 
The final safety input is the maximum between the original safety risk and a scaled predictive risk. 
In addition, a same-state candidate-pair ranking regularizer is used to discourage the discriminator/reward model from assigning higher rewards to riskier candidate actions than safer alternatives.
```

中文版本：

```text
我们保留原始动作相关安全分支，但只以 1 维标量形式输入判别器；
不再使用原始 legacy safety aux 作为主要纠正项；
而是用 1.0s 预测风险作为 residual 修正原始安全标量；
同时使用同状态候选动作对比，使危险动作不应比安全候选动作获得更高 reward。
```

如果你想更诚实地反映最终采用版本，可以加一句：

```text
后续尝试的 CandidateV2、focused CPair 和 generator risk penalty 没有稳定超过该配置，因此最终采用 P30 的 scalar-only residual safety design。
```

# 14. 最后再强调一次当前代码注意点

**历史 P30 = scalar-only + predictive residual + CPair additive。**
**当前 GitHub main 直接跑出来是否等于历史 P30，需要先确认 `safety_cpair_additive_weight` 是否真的被设置为 0.03。**

你现在仓库里这段很可疑：

```text
if enable_predictive_safety_residual and predictive_safety_enable_cpair_additive:
    safety_cpair_additive_weight = 0.0
```

并且没有搜到 `airl_trainer.safety_cpair_additive_weight` 的后续更新。若这没有被 Codex 修掉，那么当前重跑版 P30 实际上就会变成：

```text
old scalar + predictive residual
但没有 CPair additive loss
```

那就和历史 P30 不一致。这个点建议你让 Codex 加一个日志字段：

```text
effective_cpair_weight
```

并在每个 epoch 打印。如果它不是 0.03，先不要跑消融。([GitHub][1])

[1]: https://github.com/xuejiali1234/gym-airl-copy/blob/main/train_airl_baseline.py "gym-airl-copy/train_airl_baseline.py at main · xuejiali1234/gym-airl-copy · GitHub"
[2]: https://github.com/xuejiali1234/gym-airl-copy/blob/main/model/safety_q_module.py "gym-airl-copy/model/safety_q_module.py at main · xuejiali1234/gym-airl-copy · GitHub"
[3]: https://github.com/xuejiali1234/gym-airl-copy/blob/main/model/safety_airl.py "gym-airl-copy/model/safety_airl.py at main · xuejiali1234/gym-airl-copy · GitHub"
