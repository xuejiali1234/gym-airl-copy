## 1. GC-AIRL 原文到底有没有说“和哪篇具体论文的 GAIL/LSTM/RL 对比”？

**没有严格说“我复现了某某论文的代码/模型”。**
它的写法更像是：作者自己实现了几个 benchmarking models，然后引用对应方法来源或设计依据。

具体是：

**GAIL baseline**
文中说 GAIL 是 Ho and Ermon 2016 提出的 IL 算法，区别于 AIRL 的地方是：GAIL 直接构造一个 MLP discriminator，并把 discriminator 的 log 值作为 reward。它还提到 GAIL 已被用于交通问题，比如 Choi et al. 2021 的 TrajGAIL，但并没有说它直接用了 TrajGAIL 的代码或配置。

**RL baseline**
文中说 RL baseline 是一个基于手工 reward 的 PPO 模型，reward 由 efficiency、safety、comfort、goal 四部分组成，设计“motivated by Yu et al. 2022; Zhu et al. 2020”。也就是说，它不是复现某一篇 RL paper 的完整代码，而是参考文献里的 reward idea，自建了一个 PPO baseline。

**LSTM baseline**
文中说 LSTM 模型和 GC-AIRL 使用相同输入输出规格，网络是两层 LSTM，每层 256 hidden units，dropout 0.2，用 MSE 和 RMSprop 训练。它引用的是经典 LSTM 论文 Hochreiter and Schmidhuber 1997，而不是说复现了某个 NGSIM LSTM 项目。

所以你论文里如果参考它，最稳的说法是：

> Following Sun and Yang (2024), we compare the proposed model with several standalone baselines, including GAIL, PPO-based RL with a handcrafted driving reward, and supervised LSTM/BC trajectory/action prediction.

不要写成：

> 我们复现了 Sun and Yang 论文中的 GAIL/RL/LSTM 代码。

因为原文也不是这个口径。

---

## 2. GC-AIRL 论文里的 benchmark 结果可以怎么借鉴？

它的 Table 2 给了一个很好的参照：GC-AIRL 和 AIRL、GAIL、RL、LSTM 比较。GC-AIRL merging success rate 是 `0.815`，collision rate 是 `0.030`；AIRL 是 `0.680 / 0.151`；GAIL 是 `0.646 / 0.163`；RL 是 `0.768 / 0.125`；LSTM 是 `0.796 / 0.147`。

这说明两点：

1. **非 AIRL baseline 很值得放**，尤其是 GAIL、RL、LSTM。
2. **成功率和碰撞率不一定单调一致**。比如 LSTM 成功率不低，但 collision 仍然较高；RL 成功率也不低，但 safety / comfort trade-off 复杂。

它的 Table 3 也很有启发：G-AIRL 只加 goal 后 success 提升到 `0.776`，但 collision 是 `0.161`；C-AIRL 是 `0.739 / 0.206`；完整 GC-AIRL 才到 `0.815 / 0.030`。也就是说，**单独加模块不一定降低碰撞，组合后才可能出现最低 collision**。这和你现在遇到的模块交互现象是对得上的。

---

## 3. 我建议你优先放哪些非 AIRL baseline？

按“可复现性优先”排序，我建议你不要贪多，优先放这 4 类。

---

# A. LSTM / BC 行为克隆 baseline：最稳、最容易复现

这是最推荐的第一个非 AIRL baseline。

你可以直接实现成：

```text
输入：state 或 state+goal
输出：连续动作 ax, ay
loss：MSE(action_pred, expert_action)
模型：MLP / LSTM / GRU 都可以
```

最简单版本是 **BC-MLP**：

```text
state(+goal) -> MLP -> action
```

稍强版本是 **BC-LSTM**：

```text
past state sequence(+goal) -> LSTM -> action
```

这类 baseline 很重要，因为它回答：

> 不用 AIRL、不用对抗训练，单纯监督学习能做到什么？

GC-AIRL 原文的 LSTM baseline 就是这个思路：相同输入输出规格，两层 256 hidden LSTM，dropout 0.2，MSE，RMSprop。

如果你想找代码参考，可以看 HRC-LSTM。它是 NGSIM I-80 上同时建模 car-following 和 lane-changing 的 LSTM 项目，虽然不是你的 ramp merging 完全同场景，但代码和数据处理思路很接近。([GitHub][1])

**我建议论文里写：**

> We include a supervised LSTM/behavior cloning baseline with the same state-action specification to assess whether adversarial reward learning is necessary beyond direct action imitation.

---

# B. PPO / RL handcrafted reward baseline：最容易和你当前环境对齐

第二个推荐是 PPO-RL baseline。

它不是 AIRL，也不需要专家动作监督，只需要你在当前环境里定义一个 reward：

```text
r = efficiency + safety + comfort + goal
```

这正好和 GC-AIRL 原文的 RL baseline 对齐。它的 reward 包括 longitudinal speed、TTC safety、2D jerk comfort、distance-to-goal 四部分，并用 PPO 直接学习 policy。

你可以在你自己的环境里实现，不必复刻外部环境。这样最公平，因为：

```text
同样的 state
同样的 action
同样的 episode
同样的 collision / endpoint / merge 指标
```

如果你要参考外部代码，HighwayEnv 是最成熟的自动驾驶 RL 环境之一，有 highway、merge、roundabout 等任务，并且和 Stable-Baselines3/PPO 结合很方便。([GitHub][2])

但我建议你不要直接拿 HighwayEnv 的结果当论文主 baseline，因为它的 merge-v0 和你的 NGSIM 2D merging 任务不完全一样。更好的做法是：

```text
用 SB3 PPO 或你已有 PPO trainer，
在你的 GYM merging 环境里训练 RL baseline。
```

**论文中可以写：**

> We implement a PPO-based RL baseline with a handcrafted driving reward consisting of efficiency, safety, comfort, and goal-reaching terms, following the benchmark design in Sun and Yang (2024).

---

# C. GAIL baseline：非 AIRL，但和 AIRL 很接近

第三个推荐是 GAIL。

GAIL 是非 AIRL，但仍属于 adversarial imitation learning。它可以回答：

> 是 AIRL 的 reward recovery 机制有用，还是普通 adversarial imitation 就够？

GC-AIRL 原文也把 GAIL 放进 benchmark。它说 GAIL 直接用 MLP discriminator，并使用 discriminator log value 作为 reward。

代码方面，最省事的是用 `imitation` 库。Stable-Baselines3 文档明确提到 `imitation` 库实现了 BC、DAgger、AIRL、GAIL、DRLHP 等算法。([Stable-Baselines3 文档][3])

也可以参考 TrajGAIL。它有公开 GitHub，包含 TrajGAIL、MaxEnt IRL、RNN trajectory generator 等实现。([GitHub][4])
不过 TrajGAIL 原本更偏城市轨迹生成，不是 ramp merging 连续控制，适合作为 GAIL 代码参考，不一定适合直接迁移。

**论文中可以写：**

> GAIL is included as a non-AIRL adversarial imitation baseline to evaluate whether the proposed reward regularization improves over a discriminator-reward imitation framework without AIRL-style reward decomposition.

---

# D. IDM + MOBIL rule-based baseline：可解释、复现性高，但要谨慎

如果你想加一个非学习 baseline，可以考虑 IDM + MOBIL。

它优点是：

```text
可解释
不需要训练
复现性极高
可以作为传统交通模型 baseline
```

HighwayEnv 里就有基于 IDM 的 longitudinal control 和 MOBIL 的 lane-change decision 逻辑；官方代码里说明了 IDM 计算前车距离/速度下的加速度，MOBIL 决定是否换道。([GitHub][5])

但它的缺点是：

```text
不一定天然生成你需要的 NGSIM 2D continuous merging trajectory；
可能需要你把它接到你的环境里；
它更像传统 rule-based baseline，而不是 learning baseline。
```

所以它适合放成附加 baseline，而不是必须。

**论文中可以写：**

> A rule-based IDM+MOBIL baseline is included as a classical non-learning reference when applicable, but it is not directly optimized from trajectory demonstrations.

---

## 4. 我最推荐你的最终 baseline 组合

如果你想简单、严谨、可复现，我建议最终放：

```text
1. AIRL Base
2. GAIL
3. BC-LSTM 或 BC-MLP
4. PPO-RL handcrafted reward
5. Proposed P30
```

如果还有余力，再加：

```text
6. IDM+MOBIL rule-based baseline
```

其中你问的是“非 AIRL baseline”，那就是：

```text
GAIL
BC-LSTM / BC-MLP
PPO-RL
IDM+MOBIL
```

---

## 5. 不建议优先做哪些 baseline？

我不建议你现在优先做这些：

```text
1. 复杂 Transformer trajectory prediction baseline
2. 多智能体 RL baseline
3. safe RL / CBF-RL 大系统
4. 需要 CARLA / SUMO 大环境迁移的 baseline
```

原因是你的主问题已经够复杂了：AIRL + Goal + Attention + Safety + seed sensitivity + signal-zero ablation。再加一堆难复现 baseline，会把论文拖散。

如果你想找 safe RL 参考代码，可以看 On_Ramp_Merge_Safe_RL。它是 highway-env-based 的 on-ramp merging safe RL 项目，有官方实现。([GitHub][6])
但它更适合作为参考，不一定适合作为你论文里的主 baseline。

---

## 6. 推荐你在论文里这样写 baseline 选择

可以写成：

> Following the benchmark design of Sun and Yang (2024), we compare the proposed model with three non-AIRL baselines: a GAIL baseline, a PPO-based RL baseline with a handcrafted driving reward, and a supervised LSTM/behavior cloning baseline. GAIL evaluates whether adversarial imitation without AIRL reward decomposition is sufficient. PPO-RL evaluates the performance of direct reward engineering. LSTM/BC evaluates direct action imitation without adversarial reward learning.

中文：

> 参考 Sun and Yang (2024) 的 benchmark 设计，本文设置三类非 AIRL 基线：GAIL、基于手工驾驶奖励的 PPO-RL，以及监督式 LSTM/行为克隆模型。GAIL 用于评估不采用 AIRL 奖励分解时的对抗模仿学习效果；PPO-RL 用于评估直接奖励工程的性能；LSTM/BC 用于评估不进行对抗奖励学习的直接动作模仿能力。

---

## 7. 如果你只想最小化工作量

那我建议你只做两个：

```text
BC-LSTM / BC-MLP
PPO-RL
```

原因：

```text
BC 最容易复现；
PPO-RL 和 GC-AIRL 原文对齐；
两者一个是监督学习，一个是强化学习，覆盖面够。
```

GAIL 虽然重要，但实现和调参成本比 BC/RL 高，而且和 AIRL 太接近。如果时间紧，可以只在文中引用 GC-AIRL 论文的 GAIL 结果作为相关工作，不一定自己复现。

不过如果论文需要“和非 AIRL imitation baseline 比”，那就加 GAIL。

---

## 8. 最终建议

我的排序是：

```text
必做：
  1. BC-LSTM 或 BC-MLP
  2. PPO-RL handcrafted reward

建议做：
  3. GAIL

可选：
  4. IDM+MOBIL rule-based baseline
  5. safe RL on-ramp baseline
```

一句话：**GC-AIRL 原文没有严格复现某个 GAIL/RL/LSTM 代码，而是自建了三类 baseline；你最稳的做法也是在自己的环境中实现 GAIL、PPO-RL、BC-LSTM/BC-MLP，保证同数据、同动作空间、同评价指标，比追求外部代码完全复刻更重要。**

[1]: https://github.com/tjzxh/HRC-LSTM?utm_source=chatgpt.com "tjzxh/HRC-LSTM"
[2]: https://github.com/Farama-Foundation/HighwayEnv?utm_source=chatgpt.com "Highway-Env"
[3]: https://stable-baselines3.readthedocs.io/en/master/guide/imitation.html?utm_source=chatgpt.com "Imitation Learning — Stable Baselines3 2.9.0a2 documentation"
[4]: https://github.com/benchoi93/TrajGAIL?utm_source=chatgpt.com "benchoi93/TrajGAIL"
[5]: https://github.com/eleurent/highway-env/blob/master/highway_env/vehicle/behavior.py?utm_source=chatgpt.com "HighwayEnv/highway_env/vehicle/behavior.py at master"
[6]: https://github.com/wenqing-2021/On_Ramp_Merge_Safe_RL?utm_source=chatgpt.com "wenqing-2021/On_Ramp_Merge_Safe_RL: we combine ..."
