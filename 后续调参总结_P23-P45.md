# 后续调参总结：P23-P45

本文是 `历史调参总结.md` 的续篇，不修改原文档。总结范围从 `U220_D230_Decay250/Decay255` 之后开始，覆盖 2026-04-28 到 2026-05-06 的后续探针、诊断、复评和结论。

写法采用结论优先：先说明当前保留方案，再解释为什么最终走到 `P30 @298`，以及哪些路线已经不建议继续。

## 1. 当前结论

当前主 reference 是：

```text
P30_CPairD250_NoLateLR_Save1 @298
```

它不是训练内最后一轮模型，而是 292-298 后段窗口里综合表现最稳的一点。固定协议复评中，`P30 @298` 的核心结果是：

| 协议 | merge | endpoint | safety | collision |
| --- | ---: | ---: | ---: | ---: |
| full217 | 0.7742 | 0.9585 | 0.8387 | 26/217 |
| hard15 | 0.4000 | 0.9333 | 0.2000 | 11/15 |

补充候选是：

```text
P30_CPairD250_NoLateLR_Save1 @292
```

它在 full217 碰撞略高，但 endpoint 更高，并且 hard15 碰撞更低：

| 协议 | merge | endpoint | safety | collision |
| --- | ---: | ---: | ---: | ---: |
| full217 | 0.7788 | 0.9816 | 0.8387 | 31/217 |
| hard15 | 0.4000 | 0.9333 | 0.2667 | 10/15 |

`P30` 原始配置已经用 `P30_CPairD250_NoLateLR_Save1_Rerun` 完全复现：关键训练指标逐点一致，说明后续代码改动没有污染 `P30` 的训练配置。

不替代 `P30` 的路线已经比较清楚：

- `P45 smoothing`：没有改善后段窗口，反而显著抬高 292-298 的 collision。
- `P42/P43/P44 generator penalty`：要么没有触发，要么触发后打坏 `P30` 后期窗口。
- `focused CPair` / `task-safe` / `CandidateV2`：有局部安全信号，但没有在 fixed protocol 上超过 `P30`。
- `v10b`：是测试时控制层，不是训练内最优主线。

## 2. 当前保留模型

| 模型点 | 定位 | 训练内表现 | fixed protocol 结论 | 模型目录 |
| --- | --- | --- | --- | --- |
| `P30 @298` | 当前主模型 | merge 0.89, endpoint 0.96, safety 0.93, collision 0.03 | full217 `26/217`，endpoint 0.9585，safety 0.8387；hard15 `11/15` | `train_log/baseline_attn_goal_safe_branch_aux_probe_P30_CPairD250_NoLateLR_Save1_20260502_215110` |
| `P30 @292` | 补充候选 | merge 0.91, endpoint 0.98, safety 0.94, collision 0.04 | full217 `31/217`；hard15 `10/15`，更适合作为 hard case 对照 | `train_log/baseline_attn_goal_safe_branch_aux_probe_P30_CPairD250_NoLateLR_Save1_20260502_215110` |
| `P40 @274` | safety-only reference | 训练内安全信号强 | full217 `21/217`，但 endpoint 0.9447 低于 0.95，hard15 `12/15`，不能做综合主模型 | `train_log/baseline_attn_goal_safe_branch_aux_probe_P40_P30_FocusedCPairLead_20260505_030854` |

因此，论文/答辩主报告建议以 `P30 @298` 为主，`P30 @292` 作为补充候选，`P40 @274` 只作为“更安全但任务完成度不足”的诊断对照。

## 3. 后续调参阶段

| 阶段 | 核心动作 | 主要结论 |
| --- | --- | --- |
| v10b / I80 / SUMO | 做 I80 迁移测试、轨迹绘制、SUMO 合流 PoC；`v10b` 接在测试时 | `v10b` 是测试时策略控制或屏蔽层，不是训练出的新主模型。它能改善部署侧行为，但不作为训练主线最优结论。 |
| P1/P2/P3 Predictive Safety | 引入 `PredictiveSafetyCritic`、1.0s rollout、risk/critical head、predictive scalar 和 regulator | 训练骨架打通，但初版 risk 和 batch-level regulator 偏强，容易惩罚正常合流，收益不足。 |
| Offline risk 诊断 | 先做离线风险诊断，再把 fixed gap 改为 target-lane gap-aware、speed-aware；温和参数 `lead_tau=0.5`、`follow_tau=0.4` | 证明原始 predictive risk 对正常合流过度敏感，需要按目标车道间隙和速度调整。 |
| Probe A/B | 使用 old safety + predictive residual，保留 legacy aux；再加入 Decay250 / CPair | 逐步确认有效方向不是纯 predictive safety，而是旧安全模块作为底座，predictive residual 作为补充。 |
| P25-P30 | 围绕 `CPair additive 0.03`、`Decay250`、late schedule、late gen LR 做组合 | 最终形成 `P30_CPairD250_NoLateLR_Save1`。有效组合是 `CPair additive 0.03 + Decay250 + no late gen LR`。 |
| P31-P37 | 做 CandidateV2、lead-aware、task-safe、Warmup001、late/ramp、统一复评 | 有局部优势，但 fixed protocol 没有超过 `P30`。Warmup001 可作为诊断线索，不作为主模型。 |
| P38-P41 | 继续 task-safe/focused CPair | `P40` 产生 safety-only 信号，full217 collision 可到 `21/217`，但 endpoint 和 hard15 不够，不能替代 `P30`。 |
| P42-P44 | 尝试 generator high-risk penalty | `P43` 证明总风险阈值 0.7 基本空转；`P44` lead-only 可以触发，但会破坏 `P30` 的 292-298 后段窗口。 |
| P45 + P30 rerun | 测 smooth transitions，并重跑 P30 原始探针 | `P45` smoothing 失败；`P30 rerun` 完全复现，确认 `P30` 结论可靠。 |

## 4. P30 为什么有效

`P30` 的关键不是单独某个新模块，而是一组比较克制的组合：

```text
old safety
+ predictive residual scale = 0.5
+ candidate-pair additive = 0.03
+ Decay250
+ D230 late n_disc = 4
+ U220 light unfreeze
```

同时它关掉了几条后来被证明风险较大的路径：

- 不开 full predictive safety critic 主导训练。
- 不开 generator risk penalty。
- 不开 feature fusion。
- 不接 `v10b` teacher / shield 训练。
- 不用 late generator LR drop。
- 不把 `@300` 当主模型。

简单说，`P30` 保留了旧安全模块的稳定性，只让 predictive residual 和 CPair 在判别器侧轻轻塑形；再用 `Decay250` 降低后期安全分支学习率，减少后段震荡。

## 5. 关键探针阶段表

| 探针段 | 代表配置 | 作用 | 结果 |
| --- | --- | --- | --- |
| P1/P2/P3 | predictive scalar / weak regulator / normal regulator | 验证 predictive safety 训练骨架 | 骨架可用，但初版 risk/regulator 太硬，不作为主线。 |
| Probe A/B | old safety + predictive residual + legacy aux | 回到旧安全底座，叠 predictive residual | 比纯 predictive safety 稳，是后续 CPair/Decay 的基础。 |
| P25 B_D250 / CPair | `B + Decay250`、`B_CPair003` | 分别测试 Decay 和 CPair | CPair 有效，但需要和 Decay250 组合才更稳。 |
| P26/P27 | late schedule、CPair additive、late gen LR | 看后期能否进一步稳住 | late schedule/late gen LR 没有稳定超过 P30 思路。 |
| P30 | `CPair003 + Decay250 + no late LR + save1` | 找 291-299 真实最佳点 | 成为当前主 reference，@298 fixed protocol 最稳。 |
| P31-P37 | CandidateV2、lead-aware、task-safe、Warmup001 | 诊断 CPair 到底选什么 pair，以及是否更 task-aware | 没有在 fixed protocol 上替代 P30。 |
| P38-P41 | focused CPair | 强化 lead/focused pair | P40 有 safety-only 价值，但综合指标不足。 |
| P42-P44 | generator penalty | 尝试从 generator 侧压高风险 | 空转或破坏后期窗口，不继续。 |
| P45 | smooth transitions | 让 safety/CPair 权重变化更平滑 | collision 明显变差，失败。 |

## 6. 失败路线归纳

| 路线 | 为什么不继续 |
| --- | --- |
| 纯 predictive safety scalar | 初版 risk 对正常合流惩罚过强，容易把任务推进打掉。 |
| batch-level ranking regulator | batch 内 safe/risky 分布不稳定，信号粗，后来改为更局部的 residual/CPair 思路。 |
| CandidateV2 / raw-rank / task-safe | 诊断价值有，但没有超过 `P30` fixed protocol，且容易牺牲 endpoint/merge。 |
| focused CPair | 可以压 full217 collision，但 hard15 和 endpoint 不够，适合做 safety-only reference。 |
| generator high-risk penalty | 总风险阈值 0.7 在 `P43` 基本空转；lead-only 在 `P44` 触发后又破坏后期窗口。 |
| smoothing schedule | `P45` 没有让曲线更可用，反而使后段 collision 变差。 |
| late generator LR drop | 目标是保住 @295，但没有比 no-late-lr 的 P30 更稳。 |
| 把 `@300` 当主模型 | P30 @300 出现终点和合流塌陷，不能因为它是最后 checkpoint 就选它。 |
| 把 `v10b` 写成训练方案 | `v10b` 是测试时控制策略，不是训练内主模型。 |

## 7. 后续建议

后续不建议再大规模调参。当前主线已经比较清楚：论文和答辩优先报告 `P30 @298` 的 fixed protocol 结果，同时保留 `P30 @292` 作为 hard15 补充候选。

训练曲线展示建议使用 raw curve + EMA smoothing，而不是为了让曲线好看继续修改训练 schedule。后续如果还要做，只建议做小范围工作：

- 对 `P30 @292/@298` 做更多 seed 或 fixed protocol 复评。
- 对 `P40 @274` 只做 safety-only 解释，不升级为主模型。
- 对 CPair 只做日志诊断或论文解释，不再引入大结构。
- 对 `v10b` 放在测试时控制和可视化部署章节，不放进训练主线结论。

## 8. 关键日志索引

主要训练与复评目录如下：

```text
train_log/baseline_attn_goal_safe_branch_aux_probe_P30_CPairD250_NoLateLR_Save1_20260502_215110
train_log/ReEval_P30_KeyCkpts_20260503
train_log/baseline_attn_goal_safe_branch_aux_probe_P30_CPairD250_NoLateLR_Save1_Rerun_20260506_131051
train_log/baseline_attn_goal_safe_branch_aux_probe_P40_P30_FocusedCPairLead_20260505_030854
train_log/ReEval_P40_KeyCkpts_20260505
train_log/baseline_attn_goal_safe_branch_aux_probe_P42_P30_LateTinyHighRiskPenalty270_20260505_151655
train_log/baseline_attn_goal_safe_branch_aux_probe_P43_P30_LateTinyHighRiskPenalty270_L006_20260505_204041
train_log/baseline_attn_goal_safe_branch_aux_probe_P44_P30_LateTinyTargetLeadPenalty270_20260505_215601
train_log/baseline_attn_goal_safe_branch_aux_probe_P45_P30_SmoothTransitions_20260506_114332
```

关键复评口径：

```text
P30 @298: full217 26/217, hard15 11/15
P30 @292: full217 31/217, hard15 10/15
P40 @274: full217 21/217, hard15 12/15
```

最终采用口径：

```text
主模型：P30_CPairD250_NoLateLR_Save1 @298
补充候选：P30_CPairD250_NoLateLR_Save1 @292
safety-only reference：P40 @274
```
